name: Build and test

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.425
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: dotnet restore MistoPodii.sln
      - run: dotnet build MistoPodii.sln --no-restore -c Release
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:e2e
